# =============================================================================
# 澳洲股市周报 - 环境变量配置示例
# =============================================================================

# -----------------------------------------------------------------------------
# Gmail SMTP 配置（可选；未配置时仍会生成本地/Pages报告）
# -----------------------------------------------------------------------------
GMAIL_ADDRESS=your_email@gmail.com
GMAIL_APP_PASSWORD=your_app_password_here

# -----------------------------------------------------------------------------
# 收件人邮箱配置
# -----------------------------------------------------------------------------
# 留空则发送给发送者自己
# 多个收件人用逗号分隔，例如: alice@example.com,bob@example.com
RECIPIENT_EMAIL=

# -----------------------------------------------------------------------------
# 可选配置
# -----------------------------------------------------------------------------
# 本地运行输出目录（默认: ~/Downloads/asx_weekly_report）
# OUTPUT_DIR=/path/to/your/output/directory

# 是否启用调试模式（默认为 false）
# DEBUG=false
