# =============================================================================
# 澳洲股市投资周报 - 环境变量配置示例
# =============================================================================
# 说明：复制此文件为 .env 并填入您的真实配置信息
# 警告：.env 文件包含敏感信息，切勿提交到 Git 或分享给他人
# =============================================================================

# -----------------------------------------------------------------------------
# Z.ai GLM API 配置
# -----------------------------------------------------------------------------
# 获取地址: https://open.bigmodel.cn/
# 推荐模型: glm-4-flash (快速且经济)
ZAI_API_KEY=your_zai_api_key_here

# -----------------------------------------------------------------------------
# Gmail SMTP 配置
# -----------------------------------------------------------------------------
# Gmail 应用密码获取方式:
# 1. 访问 https://myaccount.google.com/security
# 2. 启用两步验证
# 3. 在"两步验证"下方找到"应用密码"
# 4. 创建一个新的应用密码（16位，无空格）
GMAIL_ADDRESS=your_email@gmail.com
GMAIL_APP_PASSWORD=your_app_password_here

# -----------------------------------------------------------------------------
# 收件人邮箱配置
# -----------------------------------------------------------------------------
# 留空则发送给发送者自己
# 多个收件人用逗号分隔，例如: alice@example.com,bob@example.com
RECIPIENT_EMAIL=

# -----------------------------------------------------------------------------
# 可选配置
# -----------------------------------------------------------------------------
# 报告输出目录（默认为 ~/Downloads/asx_weekly_report）
# OUTPUT_DIR=/path/to/your/output/directory

# Z.ai API 模型选择（默认为 glm-4.7）
# 可用模型: glm-4.7, glm-4-plus, glm-4-air 等
# ZAI_MODEL=glm-4.7

# 是否启用调试模式（默认为 false）
# DEBUG=false
